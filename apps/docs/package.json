{
	"name": "@aurilabs/docs",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "auri docs + demo site — rendered by the catalog it documents.",
	"license": "Apache-2.0",
	"author": "Hugo Pretorius <ohheyhugo@gmail.com>",
	"scripts": {
		"sync-contract": "node scripts/sync-contract.js",
		"dev": "npm run sync-contract && vite dev",
		"build": "npm run sync-contract && vite build",
		"preview": "vite preview",
		"check": "npm run sync-contract && svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
	},
	"devDependencies": {
		"@aurilabs/core": "*",
		"@aurilabs/ops": "*",
		"@sveltejs/adapter-static": "^3.0.10",
		"@sveltejs/kit": "^2.49.5",
		"@sveltejs/vite-plugin-svelte": "^6.0.0",
		"@types/node": "^24.13.3",
		"svelte": "^5.55.7",
		"svelte-a2ui": "^0.1.0",
		"svelte-check": "^4.1.0",
		"typescript": "^5.6.0",
		"vite": "^6.0.0"
	}
}
